# MasterThesis

At the moment all coding files work. The dense_IR_example with and without cuda is the same as dpr_example.ipynb just as a .py file. 
Same goes with sparse_IR_example which is the same as the sparse examples in example.ipynb just as a .py file.

